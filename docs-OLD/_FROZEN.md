# This tree is FROZEN
 
Do not add new files here. The canonical docs tree is now docs-new/
(to become docs/ after Owner/Operator approves the swap).
 
This old flat tree is read-only and will move to
docs-new/99_ARCHIVE/legacy/old-flat-tree/ once re-homing and the
review-flagged files are resolved.
