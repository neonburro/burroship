# dependencies
node_modules
.yarn/cache
.yarn/install-state.gz

# build output
dist
.netlify

# env
.env
.env.local
.env.*.local

# editor
.vscode
.idea
.DS_Store

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# generated tokens (when we promote DESIGN.md to executable later)
src/styles/tokens.css

# Finder duplicates. macOS names a copied file or folder "thing 2", which slips
# past a rule written for "thing". A 40MB "dist 2" build folder rode into the
# studio repo this way. Same guard here before it happens.
*\ 2/
*\ 2.*
